{
  "name": "globetrotters.earth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "debug": "npm run clean && DEBUG=* eleventy",
    "watch:eleventy": "eleventy --serve --incremental",
    "build:eleventy": "eleventy",
    "build": "npm-run-all clean build:eleventy",
    "start2": "sh dato.sh && npm-run-all clean --parallel watch:*",
    "start": "npm-run-all clean --parallel watch:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "dotenv": "^16.4.7",
    "firebase": "^11.1.0",
    "node-fetch": "^3.3.2",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.0.1"
  }
}
